#!/bin/bash

exec 2>&1

export JIRA_HOME="<%=@options[:jira_home]%>"

cd <%=@options[:directory]%>

exec chpst -u <%=@options[:user]%>:<%=@options[:group]%> -U <%=@options[:user]%>:<%=@options[:group]%> bin/start-jira.sh -fg
